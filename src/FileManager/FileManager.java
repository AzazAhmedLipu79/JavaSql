package FileManager;

/**
 * Deprecated: FileManager is no longer used. All file I/O is handled by Util.TableEngine
 * and CatalogManager. This class remains only to avoid breaking references in older docs.
 */
public final class FileManager { private FileManager() {} }
